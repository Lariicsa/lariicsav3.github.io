import React from 'react'

const Info = ({children}) => (
    <div className="row center lg animated">
        <div className="col lg">
            {children}
        </div>
    </div>
)

export default Info